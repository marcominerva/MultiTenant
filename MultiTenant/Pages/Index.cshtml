@page "/"
@model IndexModel

@{
    ViewData["Title"] = "Home page";
}

<div class="row">
    <div class="col-12 text-center">
        <h1>@Model.Tenant?.Name</h1>
        <img src="@Model.Tenant?.Image" class="img-fluid rounded" style="max-height: 200px" />
    </div>
</div>